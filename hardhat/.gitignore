# Node modules
/node_modules

# Compilation output
/dist

# pnpm deploy output
/bundle

# Hardhat Build Artifacts
/artifacts

# Hardhat compilation (v2) support directory
/cache

# Typechain output
/types

# Hardhat coverage reports
/coverage


.env

/generated

# Local anvil deployment records - regenerated on every local deploy, pure
# noise for version control (unlike the real testnet ones, which are the
# canonical record of what's actually live on those chains).
/deployments/localhost
/deployments/31337

/nginx

docker-compose.prod.yaml

Dockerfile.prod